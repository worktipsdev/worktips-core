#define DEF_WORKTIPS_VERSION_MAJOR 7
#define DEF_WORKTIPS_VERSION_MINOR 1
#define DEF_WORKTIPS_VERSION_PATCH 10

#define WORKTIPS_STRINGIFY2(val) #val
#define WORKTIPS_STRINGIFY(val) WORKTIPS_STRINGIFY2(val)
#define DEF_WORKTIPS_VERSION_STR WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_MAJOR) "." WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_MINOR) "." WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_PATCH)

#define DEF_WORKTIPS_VERSION_TAG "@VERSIONTAG@"
#define DEF_WORKTIPS_RELEASE_NAME "Valiant Vidar"
#define DEF_WORKTIPS_VERSION_FULL DEF_WORKTIPS_VERSION_STR "-" DEF_WORKTIPS_VERSION_TAG

#include "version.h"

const std::array<uint16_t, 3> WORKTIPS_VERSION = {DEF_WORKTIPS_VERSION_MAJOR, DEF_WORKTIPS_VERSION_MINOR, DEF_WORKTIPS_VERSION_PATCH};

const char* const WORKTIPS_VERSION_TAG = DEF_WORKTIPS_VERSION_TAG;
const char* const WORKTIPS_VERSION_STR = DEF_WORKTIPS_VERSION_STR;
const char* const WORKTIPS_RELEASE_NAME = DEF_WORKTIPS_RELEASE_NAME;
const char* const WORKTIPS_VERSION_FULL = DEF_WORKTIPS_VERSION_FULL;
